if [[ -f $HOME/projects/company/CoB/utils/cob/bin/cob ]]; then
    eval "$($HOME/projects/company/CoB/utils/cob/bin/cob init -)"
fi

if [[ -f $HOME/dotfiles/dev-utils/bin/dev ]]; then
    eval "$($HOME/dotfiles/dev-utils/bin/dev init -)"
fi

if [[ -f $HOME/projects/company/enear/rituala/mgc-utils/bin/mgc ]]; then
    eval "$($HOME/projects/company/enear/rituala/mgc-utils/bin/mgc init -)"
fi

if [[ -f $HOME/projects/company/enear/rituala/mgc-utils/bin/mgc ]]; then
    eval "$($HOME/projects/company/enear/rituala/mgc-utils/bin/mgc init -)"
fi

if [[ -f $HOME/projects/company/equalexperts/plusgrade/utils/bin/pg ]]; then
    eval "$($HOME/projects/company/equalexperts/plusgrade/utils/bin/pg init -)"
fi

if [ -f /usr/local/etc/bash_completion.d/git-completion.bash ]; then
   . /usr/local/etc/bash_completion.d/git-completion.bash
fi

if [[ -f /usr/local/bin/aws_completer ]]; then
    complete -C '/usr/local/bin/aws_completer' aws
fi

LUNCHY_DIR=$(dirname `gem which lunchy`)/../extras
if [ -f $LUNCHY_DIR/lunchy-completion.bash ]; then
  . $LUNCHY_DIR/lunchy-completion.bash
fi
